<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUI.ServersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ListView ItemsSource="{x:Bind ViewModel.Servers}" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="local:ServerViewModel">
                <StackPanel Orientation="Horizontal">
                    <CheckBox IsChecked="{x:Bind Status, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind Type, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind Schema, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind Host, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind Port, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind ApiUrl, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                    <TextBlock Text="{x:Bind ApiKey, Mode=OneWay}" Padding="5, 0, 5, 0"/>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
