<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:servers="using:Servers"
    xmlns:notifyIcon="using:H.NotifyIcon"
    mc:Ignorable="d">

    <StackPanel>
        <ListView ItemsSource="{x:Bind _viewModel.Servers}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="servers:Server">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind Status}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.Name}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.Type}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.Scheme}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.Host}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.Port}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.ApiUri}" Padding="5, 0, 5, 0"/>
                        <TextBlock Text="{x:Bind Config.ApiKey}" Padding="5, 0, 5, 0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <notifyIcon:TaskbarIcon
            ToolTipText="Remote Control"
            IconSource="/Icons/Device.theme-dark.ico"
            MenuActivation="RightClick"
            ContextMenuMode="SecondWindow"
            x:Name="TaskbarIcon">
            <notifyIcon:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutSeparator></MenuFlyoutSeparator>
                    <MenuFlyoutItem Text="Start all" Command="{x:Bind _startAllCommand}"/>
                    <MenuFlyoutItem Text="Stop all" Command="{x:Bind _stopAllCommand}"/>
                    <MenuFlyoutSeparator></MenuFlyoutSeparator>
                    <ToggleMenuFlyoutItem Text="Autostart" Command="{x:Bind _autostartCommand}" IsChecked="{x:Bind _viewModel.IsAutostart}"/>
                    <MenuFlyoutItem Text="Add firewall rule" Command="{x:Bind _addFirewallCommand}"/>
                    <MenuFlyoutItem Text="Exit" Command="{x:Bind _exitCommand}"/>
                </MenuFlyout>
            </notifyIcon:TaskbarIcon.ContextFlyout>
        </notifyIcon:TaskbarIcon>
    </StackPanel>
</Window>
